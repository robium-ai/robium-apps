# Application registry

The canonical index of developed and published apps. Each app's `robium-app.yaml` is the
machine-readable contract (`robium-ai app list|describe|check|run` work from
a clone of this repo); the design standard is
[docs/reference-applications-design.md](docs/reference-applications-design.md).
Detailed battle scars live in each app's architecture brief and in the sibling
`robium` repository's dated learnings.

| App | Vertical | Stack | Sim | Env | Viz | Smoke |
| --- | --- | --- | --- | --- | --- | --- |
| [lego-powered-up-teleop](lego-powered-up-teleop/) | Real-robot differential-drive teleoperation | Native Python + Pygame + Bleak + Pybricks hub bridge | none; real LEGO Powered Up hub and two motors; optional Stadia controller detected over Bluetooth | uv + Python 3.12; native BLE and SDL gamepad input on Apple Silicon host | Pygame directional pad with proportional Stadia left-stick throttle, right-stick steering, D-pad, mouse, arrows, and WASD control | 35 hardware-free tests, lint, app-contract validation, rendered UI lifecycle, detected `Google Stadia Controller`, and stopped real-hub `READY` + `PING/PONG` smoke pass 2026-09-14; steering validated and throttle-only inversion applied from wheels-up evidence, final re-check pending |
| [robot-navigation](robot-navigation/) | Classical ROS navigation | ROS 2 Jazzy + Nav2 + slam_toolbox | Gazebo Harmonic; Waffle Pi in selectable House and Warehouse environments from the shared pinned-asset catalog | Pixi/RoboStack (macOS arm64) + Docker + per-visitor Cloud Run | Native Gazebo + bundled Lichtblick + configurable Robium Dashboard `.foxe` | `./app doctor` + local and Cloud Run runtime validation (2026-08-16) |
| [diffusion-policy-pusht](diffusion-policy-pusht/) | Physical AI / ML manipulation (experimental) | LeRobot 0.6.0 + pinned official 175k Diffusion Policy | untouched gym-pusht T benchmark + custom L-I-Z OOD probes | uv + Python 3.12 + native Apple MPS or baked CPU demo image; no training required | Direct Gradio frames + additive Rerun timeline; website lifecycle gateway | `./app doctor` + launcher lifecycle + `make smoke` (2) + `make demo-smoke` (3) + CPU container/site lifecycle, all passed 2026-08-22; CPU seed 1000 solved in 116 steps at 0.953 raw coverage; published reference: 65.4% / 500 episodes |
| [act-aloha-cube-transfer](act-aloha-cube-transfer/) | Physical AI / bimanual imitation learning (experimental) | LeRobot 0.6.1 + pinned official ACT 80k checkpoint | gym-aloha ALOHA Transfer Cube on MuJoCo | uv + Python 3.12 + native Apple MPS or baked CPU demo image; no training required | Direct Gradio top camera + additive Rerun joint/action/reward timeline; website lifecycle gateway | `./app doctor` + `make smoke` + actual browser + CPU container/session/API lifecycle passed 2026-08-23; MPS seed 1001 transferred in 236 steps; bounded local calibration 2/5; published reference: 83% / 500 episodes |
| [vla-pick-and-place](vla-pick-and-place/) | Language-conditioned VLA (experimental) | pinned LeRobot v0.4.4 + official Pi0.5 LIBERO checkpoint | LIBERO-Goal task 8 (Franka Panda) | uv + Python 3.10; fake local fixtures; pinned Linux/amd64 CUDA image for RunPod | capability-scoped Gradio simulator frames; eager interactive policy profile | Doctor, 48 tests, fake rollout, publication-pointer validation, no-retry evaluation startup, duplicate-click busy handling, and rebuilt CPU/GPU image contracts pass; evaluation digest `sha256:1301937a…f0c` scored 20/20 with states 0–19, seeds 1000–1019, and no retries; live digest `sha256:4613c522…606a` passed production start → ready → rollout → cancel → delete on RTX PRO 4500; bounded three-slot smoke allocated three Pods, rejected a fourth, reached 1/3 ready under concurrent cold load, fixed diagnostic/reservation races, and ended with zero Pods and reservations; user-facing reference workflow reverified 2026-08-27; immutable [public evidence](https://huggingface.co/datasets/robium/pi05-libero-goal-task-8-evidence/tree/d9908eb717d3e8d62ca7ba0820a825daf36fa7c0); bounded three-slot production sessions enabled with atomic reservations |
| [robot-teleoperation](robot-teleoperation/) | Real-robot teleoperation | ROS 2 Humble + foxglove_bridge | TurtleBot 4 hardware | Robot host + browser client | Foxglove (browser) | `make smoke` (hardware-in-the-loop) |
| [silly-turtlebot](silly-turtlebot/) ([development tutorial](silly-turtlebot/docs/case-study.md)) | Proactive visual navigation and voice interaction (experimental) | Gemini Robotics ER 2 Streaming + guarded HTTP bridge to ROS 2/Nav2, Orin DepthAI camera, and local Kokoro-82M neural TTS | Fake TurtleBot; TurtleBot 4 in Gazebo Harmonic furnished home; TurtleBot 4 Humble hardware with Orin-connected OAK-D, speaker, and paired Stadia gamepad | containerized Gemini/Lichtblick control plane + Jazzy simulation image + native Humble robot overlay + independent Orin camera/TTS/gamepad images | Minimal Lichtblick run/stop mission controls and rolling agent log in a saved three-column Mission/3D/Image+Teleop layout with revision-aware extension cache busting and one-time bundled-layout migration, with the Image panel fed by an isolated robot-side 1 FPS compressed-image relay and a bounded 16-message Foxglove live-view backlog that drops stale frames after network/render stalls, 1 FPS full-resolution Gemini images during blocking actions, idle-only compact reasoning heartbeats, local action-progress logs, exactly-once terminal tool responses with final motion state, conflict rejection, cancellation-stop safety, timestamped rolling logs, calibrated camera FOV supplied once per mission, signed Create 3 distance/speed motion without Nav2 lidar-costmap projection, arbitrary collision-checked rotation and timed arc tools, live dock/odometry/motion state, gamepad control, and guarded neural speech with USB-speaker wake padding | Thirteen fast tests + mock mission pass, including viewer revision migration, compact heartbeat filtering, unresolved-turn heartbeat suppression, persistent-connection reuse, SSE replay, connection-loss stop, duplicate safety, long-running-action image streaming, cancellation-stop safety, conflict rejection, and terminal response ordering; 2026-09-14 physical smoke passed after deploying BackUp/AssistedTeleop and calibrated OAK-D FOV; focused live checks verified the 320x180 operator preview, full 640x360 Gemini frames, and a successful padded TTS playback; the preview relay runs independently of navigation callbacks so camera latency cannot stall motion control |
| [stackchan-er2](stackchan-er2/) | Continuously listening voice-and-vision desktop companion with exclusive LEGO control (experimental) | Gemini Robotics ER 2 Streaming automatic VAD + guarded Python actions + M5Stack Arduino firmware + in-app Pybricks BLE worker and hub watchdog bridge | none; real M5Stack STACK-CHAN K151 and two-motor LEGO robot | uv + Python 3.12 on macOS; pinned project-local Arduino ESP32 toolchain; Bleak CoreBluetooth | Onboard display, 360° yaw/90° pitch head, five guarded named animations, dual microphones, speaker, green listening LEDs, model-selected single-frame GC0308 vision, and enum-only forward/backward/left/right/stop LEGO commands | Thirty-five fast tests plus mock pass; 2026-09-14 real K151 firmware/display/head/mic/speaker and supervised ER 2 tool smoke passed; 2026-09-16 firmware 0.3.0 passed repeated 250 ms mic streaming, speaker handoff, automatic mic resume, on-demand 320×240 JPEG capture with camera/internal-I2C handoff, and a physical mic → ER 2 → vision → guarded onboard-speech turn; still images bind to their matching `look` response; firmware 0.4.0 physically passed nod, shake, 90° privacy, front→rear→front captures, reset recovery, and silent model-selected nod; 2026-09-17 v0.5.0 self-contained LEGO control passed zero-power READY/PONG, all four fixed 30%/0.8 s directions with confirmed stops, silent ER 2 text selection, and real Stack Chan microphone → ER 2 → BLE left-turn → resumed-listening smoke; the hub retains an independent 400 ms watchdog |
| [quadruped-locomotion](quadruped-locomotion/) | Reinforcement-learning locomotion | Isaac Lab + rsl_rl PPO | Isaac Sim (Unitree Go2) | Remote NVIDIA GPU | Recorded checkpoint explorer + optional browser control | `make smoke` (remote GPU) |
| [mbot-data-collection](mbot-data-collection/) | Real-robot teleoperation, VLA data collection, and policy rollout | Custom Arduino bridge firmware (ATmega328P) + pyserial + pygame + LeRobot 0.6.1 datasets | none; real Makeblock mBot v1.1 (mCore) over USB CH340 | uv + Python 3.12; arduino-cli + AVR core for flashing only | pygame webcam preview with live action/duty overlay | `make smoke` (= `./app doctor`) passed on real hardware over both transports 2026-08-30: USB (CH340) and Bluetooth (Makeblock BLE, GATT 0xFFE2/0xFFE3) reach the same firmware unchanged; camera resolved by name through a measured OpenCV-index map, since OpenCV's index order and AVFoundation's device order differ and listing must never open a device (that starts a Continuity camera); Stadia gamepad detected. Flashing is USB-only with the Bluetooth module unplugged - it shares the D0/D1 UART - and an ICSP path is provided for boards with it soldered on. BLE round trip ~60 ms, so motor commands stream un-acknowledged. `--task` repeats, so several language instructions land in one dataset: the operator picks which one each episode demonstrates with a number key, the choice is sticky and never made automatically (pre-selecting the less-recorded instruction was tried and mislabeled episodes whenever it disagreed with the driver), it is fixed at episode start, per-instruction counts are shown so an imbalance stays visible, and the set is remembered in `tasks.json` for `--resume`. `./app rollout` drives the robot from a trained SmolVLA checkpoint and switches instruction live; switching resets the policy because a 50-action chunk queue would otherwise ignore the new instruction for ~2.5 s, the wire is fed by a daemon thread because the chunk-refill step, measured on the robot at 419-442 ms (vs 11 ms median for ordinary actions, mps), exceeds the 400 ms board watchdog; and the policy runs in a second thread with LeRobot Real-Time Chunking, because a synchronous loop updates the command only after the pass and so held one action ~480 ms every 2.5 s, dragging 20 Hz down to 17.4 Hz - measured over 200 ticks the worker+RTC loop has 0 ticks over period (was 1 in 50), 0.57 ms median tick work (was 11 ms) and re-plans every 0.47 s (was 2.5 s), with seam continuity 0.0096 median vs 0.0135 mid-chunk. First trained policy: 50 episodes -> SmolVLA 20k steps, train loss 0.033, but swapping the instruction moved actions only 4.2%/8.9% of the dataset action std - the two goals were collected in separate blocks, so the image predicts the intent and the words are redundant; interleave goals from identical start layouts. No unit-test suite, by maintainer preference |
