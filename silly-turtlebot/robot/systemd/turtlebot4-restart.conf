[Service]
# Wi-Fi can become usable after network-online.target on this image. The stock
# bringup exits when CycloneDDS cannot bind wlan0 and otherwise stays failed.
Restart=on-failure
RestartSec=15
